export type LoginType = {
    email: string;
    password: string;
}

export type RegisterType = {
    fName: string;
    lName: string;
    email: string;
    password: string;
    repeatPassword: string;
}
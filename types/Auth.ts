export interface LoginType {
    email: string;
    password: string;
}

export interface RegisterType {
    fName: string;
    lName: string;
    email: string;
    password: string;
    repeatPassword: string;
}